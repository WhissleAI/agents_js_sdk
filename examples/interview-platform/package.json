{
  "name": "whissle-interview-platform-example",
  "private": true,
  "type": "module",
  "description": "A small but complete Whissle app — agents from a JSON file, live calls with or without a face, past sessions with transcripts and scores.",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@whissle/sdk": "^0.3.0"
  },
  "engines": { "node": ">=20" }
}
